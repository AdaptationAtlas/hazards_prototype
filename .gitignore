.Rproj.user
.Rhistory
.RData
.Ruserdata
Data
combined_run.log
setup_output.log
timeseries_output.log
haz-processing-notes.html
.DS_Store
**/.DS_Store
**/.ipynb_checkpoints/
# Quarto build artefacts under R/misc/
R/misc/*.html
R/misc/*_files/

# NFS temp files (CGlabs network filesystem)
.nfs*

# Rcpp sourceCpp compile cache (R/2.1 §3.4 trend kernel)
R/.rcpp_cache/

# trendkernel package build artifacts (sources tracked, builds ignored)
trendkernel/src/*.o
trendkernel/src/*.so
trendkernel/src/*.dll

# Reference PDFs and exposure scratch (large binaries, not pipeline code)
references/

# wbkernel package build artifacts (sources tracked, builds ignored)
wbkernel/src/*.o
wbkernel/src/*.so
wbkernel/src/*.dll
