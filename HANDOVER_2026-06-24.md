# Session handover — 2026-06-24 (hazards_prototype / Stage-0 workout)

develop @ origin HEAD = `40d1707`. Two-session model: **macbook (this) = code/docs**,
**cglabs = run/validate/publish on live Data/**. Both push to `develop`; always
`git pull --rebase` before pushing. Memories + caveman are macbook-local only.

## 🔴 ACTION ON PETE — rotate the CDS key
A live ECMWF/CDS credential was committed in `hazards_upstream/R/01_download_data/download_AgERA5.R`
and is in git history of BOTH repos (vendored via subtree). Code de-hardcoded to
`Sys.getenv("CDS_UID"/"CDS_KEY")` (`eeea77d`) — but that does NOT scrub history.
**Rotate at https://cds.climate.copernicus.eu (account UID 63618); put new key in
~/.Renviron.** AgERA5 is load-bearing (solar-radiation input to NDWS/NDWL watbal) so
the key stays needed — can't just delete it.

## DONE this session
- **CR-093 CLOSED** (producer + publish). R/2.2 revived end-to-end on cglabs: admin_extract
  API migration, SEC1-4 fixes, gate 10/10, published to canonical `domain=climate`
  hazard-change keys, catalogued in metadata/data.json. Closure logged in climateRationale
  ISSUES.md. Brayden informed of the new dataset via **data-management#2** (item C).
- **Desert PTOT mask** (CR-093 follow-up) — LIVE + published. R/2.2 SEC1 masks
  `PTOT < 100 mm/yr` (env `PTOT_BASELINE_MIN_MM`, default 100, decided w/ Pete + sources);
  Δmm reported everywhere (unmasked). `ISSUE_cr093_nan_zeroprecip.md` CLOSED.
  Compound cut (`PTOT_DELTA_MIN_MM`) drafted, default-off, OPTIONAL/deferred.
- **R/2 perf** (earlier): Tier A timestamped section timers + pbapply silence; §5.2
  vectorizations (`USE_R2_5_2_VEC`, probe PASS); Tier D parallelism staged+gated.
- **Repo merge step 1**: vendored `AdaptationAtlas/hazards` **nexgddp** → `hazards_upstream/`
  (full-history subtree, `272bc3c`, 192 commits). Update: `git subtree pull --prefix=hazards_upstream <hazards> nexgddp`.
- **Stage-0 Phase-0 audit** (multi-agent, 110 findings) → `UPGRADE_hazards_upstream.md`.
- **Security**: CDS key de-hardcoded (`eeea77d`) — rotation pending (above).
- **Uploads flagged STALE / deferred** — `hazards_upstream/R/07_bucket_uploads/*` target the
  legacy MVP path; whole-pipeline publish revision = separate future project
  ([[project-pipeline-upload-revision]]).

## KEY FINDINGS to carry forward
- **hazards#19 NDWS-saturation ROOT CAUSE located** = `hazards_upstream/R/04_indices/fast_calc_NDWS.R`:
  soil-moisture state via global `<<-` + AVAIL recovered as lexically-last `AVAIL-*.tif`
  (resumed/out-of-order run seeds month N from wrong month) + missing `overwrite=TRUE`.
  Fix = explicit-arg state + deterministic prior-month AVAIL (yr/mn arithmetic) + stopifnot +
  overwrite=TRUE (roadmap rank 8). This IS the rebake's Stage-0 NDWS fix.
- **Stage 0 is correctness/operability/de-dup work, NOT compute** — only ONE real Rcpp
  candidate stage-wide (`createAfricaSoils.R` soil-water loop; try data.table first).
- **NEX-GDDP** raw already on cglabs + it's AWS Open Data — Stage-0 `01_download` won't
  re-run for the rebake; rank-10 download-parallelization is low priority.
- **CRITICAL rebake gotcha**: `run3`/`run5.3` are ENV-TOGGLE-ONLY — `FORCE_OVERWRITE=1` alone
  SKIPS §3 + §5.3 → `haz_risk/` won't refresh. Rebake MUST export
  `FORCE_OVERWRITE=1 RUN_R2_RUN3=1 RUN_R2_RUN5_3=1`.

## OPEN / NEXT
1. **Rotate CDS key** (Pete — above).
2. **Stage-0 workout continues** from `UPGRADE_hazards_upstream.md` 5-phase plan. Phase 1 key fix
   DONE + uploads deferred; remaining cheap Phase-1 safety: `rm -r` DRY_RUN guard (02/free-up_space),
   `stopifnot(length(fls)>0)` after file.exists filters (04/06), `overwrite=TRUE` on AVAIL writeRaster
   (04 fast_calc_*), OR→AND corruption-test fix (03/identifyCorruptedFiles). Then **Phase 2** =
   shared `.log` helper + `SKIP_/FORCE_OVERWRITE` env controls + `atlas_dirs` paths across stages.
3. **Full rebake** — cross-repo plan in `R/NEXT_FULL_REBAKE.md`. Pre-conditions: hazards#19 NDWS fix
   (Stage 0, above), exposure vintage (proto#10), CR-115 convention state (proto#11). Poultry #13 +
   perf/Tier-D ride this bake. Post-bake = CR-068 probes.
4. **Stage 1+ (this repo's R/) workout** — deferred second pass (this session did Stage 0 only).

## Open issues (sweep 2026-06-24)
- proto#13 poultry THI 79→89 — rides full rebake (DISPATCH_poultry_thi_rebake.md, blocked as standalone)
- proto#12/#9 Pattern B exposure>VoP — Brayden's stale denominator; probe-first, not our rebake fix
- proto#11 CR-115 disputed-territory — conditional on dm#3 convention
- proto#10 Delta-Method exposure extraction (Brayden) — dependency if new/2021 exposure used
- hazards#19 NDWS saturation — root cause found (above); fix in Stage 0
- dm#3 disputed convention / dm#2 STAC-CDH catalog (A/B/C) — Brayden's

## The durable docs (read these to resume)
`UPGRADE_hazards_upstream.md` (Stage-0 roadmap) · `R/NEXT_FULL_REBAKE.md` (cross-repo rebake) ·
`ISSUE_cr093_nan_zeroprecip.md` (desert, closed) · `DISPATCH_poultry_thi_rebake.md` (blocked/deferred) ·
`R/HANDOFF_cr093_r22.md` + `HANDOVER_macbook_cr093.md` (CR-093 cglabs↔macbook) ·
`R/ISSUE_r2_5_2_parallelism.md` (Tier D). Memories: project-hazards-repo-merge-intent,
project-pipeline-upload-revision, project-cr068-post-bake-probes, project-cr093-r22-consumable-state.
