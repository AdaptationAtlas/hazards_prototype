# Handover — 2026-09-17

Read this first, then `R/NEXT_FULL_REBAKE.md` if you are touching R/2 or R/3.
Previous handovers are in `archive/dispatches/`.

---

## Where things stand

**Issue #9 is fixed and live.** The notebook-facing `hazard_exposure` product was
republished 2026-09-16 with `hazard='none'` on every hazard combination, so total VoP is
computable from the file as `any + none`. Three severity tiers, sizes matched, HTTP 206
verified, previous versions backed up to `s3://digital-atlas/sandbox/backup/issue9_20260916_135730/`.
CR-068 probes on Angola came back green: every crop at or below 100% (rice was 203.6%),
zero NaN at admin0 and admin1, and admin0 equals the sum of admin1 to the dollar. #12 was
verified resolved by the same work, with zero exceedances in 5,281 admin1 × crop pairs.

The root cause is worth knowing because it hid for months. The crop nominal-USD exposure
raster is SPAM at 0.05°, the hazard stacks are 0.25°, so every crop multiply raised
`[*] extents do not match` — and `retry_risk_x_exposure` swallowed it with `try(silent = TRUE)`,
leaving stale tifs in place for the next stage to read as current. R/3 now aligns mismatched
grids mass-conservingly and **aborts** on genuine §4.1 failures.

---

## Two things to close out first

1. **Confirm and close #9.** Brayden was asked to confirm the notebook reads cleanly. Once he
   does, tell cglabs to `rm -r Data/_parked_issue9/` — it is being retained deliberately until
   then, and it is not small.
2. **KNBS (#28) is complete — tiers 16, 17 and 18 are live.** Nothing outstanding. The census and
   projection tables published clean, and the tier-16 intersect tables were re-levelled onto the
   official denominator: national `pop_total` is now exactly 47,564,296, `pop_pct` is preserved
   bit-for-bit, and every invariant passed with deviation zero. The consumer briefing for the
   notebook side is `HANDOVER_2026-09-17_ke-enso-population-schema.md`.

   Two things came out of it. **#32** is the substantive one: the per-county correction factor
   spans 0.324 to 1.394, and Mandera, Wajir and Garissa carry about 46 % of the national
   grid-versus-census gap from about 5 % of the population, with Mandera gridded at 2.68 M against
   0.87 M enumerated. That bears on the method, which assumes the grid gives the right share within
   a county and only the level is wrong. The decisive test is already possible with published data:
   compare WorldPop, an independent surface and the census at county level — **not GRID3, which is
   modelled from KNBS microcensus and so is not independent of the census**. Do not adjudicate it in
   the data; the 2019 figures for three of those counties are under judicial challenge. And **the denominator default** is still Pete's open call — the 2019 census, or a
   projection year, which is 51.96 M against 53.33 M for 2025. Switching is a seconds-long re-level.

---

## Issue #26 is DONE — published and verified 2026-09-20

It was the next major item when this handover was written; it is no longer open. The re-bake
shipped, and the live Future Projections file now carries **18 members everywhere**, with the only
other `n_models` value being 0 on rows that hold no data at all (68,112 of them, a residue proven
to exist in the pre-#26 backup). Both baseline windows are present and genuinely different, so the
1981-2014-under-a-1995-2014-label mislabel is gone. Dispatch archived.

Two things from it worth carrying forward. The evenness gate the fix introduced did its job on the
re-bake, and when it tripped the stop was respected, the "pre-existing" claim was tested against the
backup rather than assumed, and the gate was then **narrowed** so it still fails on partial
ensembles rather than bypassed. That is the fourth gate this fortnight that fired on a legitimate
state; see [[feedback_gate_design_populations]]. And `republish_A.R` hardcodes a baseline token the
producer derives, so the two agree only by coincidence — deriving it from the files present is the
fix, not renaming the publisher to match today's output.

**The next major item is now issue #29** (portable data layer / Afrilabs). P1-P5 are done and
CGlabs-verified, the Afrilabs host profile is pinned from a node report, and all 43 datasets have an
acquire recipe. What remains is the larger half: the dataset manifest, a sync mechanism, and proving
a stage end-to-end on the second machine. Note the constraint recorded from issue #27 — inside a
JupyterHub session on PASCAL `nproc` reports 80 while the cgroup caps it as low as 8, so anything
sizing workers from `detectCores()` oversubscribes by up to tenfold.

## Everything else, ranked

| Issue | State |
|---|---|
| #30 intld reference | **Fixed and gated; parked on one decision of Pete's.** The vintage mismatch is closed (worst case 6,759x -> 1.000). Three further things came out of it. The livestock rows on the live `crop-livestock_all` are **nominal USD relabelled as constant international dollars** - the July 0.4.1 bug, still live, and the explanation for the 1.198 caveat on the hazard-exposure record; republishing from the post-fix chain fixes it. The reference cannot be republished yet because the rebuilt table has ~70 % of the live row count: **not a missing boundary product but the 0.25 deg zonal grid**, where about a third of adm2 and 16 adm1 polygons never own a cell - the same mechanism as #18, now with a magnitude. **The decision: run 0.4.4's zonal step on the 0.05 deg base regardless of `climdat_source`.** Also resolved along the way: keep `unit_full`, revert the five hive columns (they are path-synthesised, not stored), and a stale local adm0 parquet (63 countries vs 55) explained the +8 rows. |
| #29 portable data layer / Afrilabs | Design brief written this session. 78 hardcoded absolute paths, five string-equality host branches. Verify the Afrilabs path discrepancy on the node before writing any path logic. |
| #13 poultry THI 79→89 | Rides the next full rebake. |
| #32 | **Escalate to Pete.** County-level population disagreement from the #28 verification, now traced to contested enumeration: the High Court quashed the 2019 census for Mandera, Wajir and Garissa (Jan 2025) and ordered the 2009 figures be used. The tier-16 product scales those counties onto a judicially challenged denominator. Presentational call, not a data fix. |
| #24, #25, #31 | Method critiques: exposure insensitive to the climate signal, crop heat thresholds, and rangeland drought via NDVI and biomass. All need evidence work before code. |
| #11 CR-115 disputed territories | On hold pending an Atlas-wide convention from Brayden. |
| #18 admin2 zonal at 0.25° | Real, value-changing, needs sign-off. |
| #10 delta-method exposure | Deferred indefinitely. Do not run. |

---

## Conventions this session earned the hard way

- **A clean exit is not a clean run.** R/3 returned exit 0 for months while every crop × usd
  multiply failed. Check `failed_*.txt`, `skipped_*.txt`, per-variable elapsed times, and output
  mtimes against the run start.
- **Never guard on a label string.** `variable != "ha"` broke silently when `ha_name` became
  `"harv-area_ha"`, sending livestock down a branch with a NULL raster. Branch on the objects
  actually supplied.
- **Gates need row populations, not one bound.** The publish gate blocked a correct product
  twice: once on a crop whose national reference is sub-dollar, once on `generic-crop`, which
  has no reference by design. Split material, immaterial and unmatched, and print with
  `signif()` — `round()` displayed 0.4 as `0` and sent the diagnosis the wrong way for a round trip.
- **Test the tidy explanation before dispatching a day-long job.** Two plausible theories about
  the intld divergence were wrong, and checking them locally cost minutes.
- **This repository is public.** No internal addresses, hostnames or mount identifiers in
  tracked files, including when quoting them to recommend their removal.
